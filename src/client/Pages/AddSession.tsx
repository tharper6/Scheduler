import * as React from 'react';

const AddSession: React.SFC<AddSessionProps> = (props) => {
    return(
        <h1>Add Session</h1>
    )
}

export interface AddSessionProps {}

export default AddSession;